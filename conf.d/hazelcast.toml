[template]
src = "hazelcast.tmpl"
dest = "/opt/hazelcast/hazelcast.xml"
keys = [
  "/self/service",
  "/containers",
  "/services",
]
check_cmd = "/etc/confd/bin/check.sh {{ .src }}"
# reload_cmd = "/opt/hazelcast/stop.sh && /opt/hazelcast/start.sh"